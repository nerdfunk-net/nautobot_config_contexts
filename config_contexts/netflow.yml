---
_metadata:
  is_active: true
  name: netflow
  weight: 1000
  description: Netflow
netflow:
  flowrecord:
  - name: FLOW-REC1
    description: desc
    match:
      ipv4:
      - tos
      - protocol
      - source address
      - destination address
  - name: FLOW-REC2
    description: desc
