---
_metadata:
  name: netflow
  weight: 1000
  description: Netflow
  is_active: true
netflow:
   flowrecord:
      name: "name"
      description: "desc"
      match:
         ipv4: ["tos","protocol"]