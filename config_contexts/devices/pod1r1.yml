---
_metadata:
  name: pod1r1
  weight: 2000
  description: specifify config
  is_active: true
# add additional config to the device
additional:
 - config: >
    extra config for pod1r1
ntp:
- ip: '2.2.2.2'
  prefer: ''
# add additional config to the interfaces
interfaces:
   - name: Port-channel 1
     config:
       - no switchport